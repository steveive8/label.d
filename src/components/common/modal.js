import React, {useState} from 'react';
import {Row, Col, Flex, Text, Image} from '../base';

export const Modal = ({}) => {
    return (
        <Row>
            <Flex>
                <Image />
            </Flex>
            <Col>
                
            </Col>
        </Row>
    )
};

export default Modal;